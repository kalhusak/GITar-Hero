const config = {
  taskListSize: 2,
  allowedCommands: [
    'init',
    'add :file:',
    'merge :branch:',
    'push',
    'commit',
    'branch',
    'checkout :branch:',
    'rebase :branch: :branch:']
};

export default config;
